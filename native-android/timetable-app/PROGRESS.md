T0.1 - Built the Kotlin/Jetpack Compose scaffold with Room and Navigation-Compose dependencies, package structure, a blank launch surface, and verified build/install/launch on ADB; assumed the blank T0.1 screen should render as an empty white Material surface.
T0.2 - Built the Room database, DAO, and all roadmap Section 3 entities with UUID string IDs, createdAt columns, foreign keys, supersededAt, and duplicatedFromId; wrote the smoke test first, observed the expected missing-schema failure, then verified insert/read/cascade behavior with :timetable-app:connectedDebugAndroidTest on device 859013c6 and :timetable-app:assembleDebug. Assumed Instant/Time/validUntil can be persisted as primitive Room columns at this scaffold stage while keeping the spec field names intact.
T0.3 - Built the Navigation-Compose skeleton with placeholder composables for Home, Institute roster settings, Time slots, Assign, Availability, Review and save, Timetable view, and History; added placeholder actions for the duplicate-from-previous and duplicate-as-new paths. Verified :timetable-app:assembleDebug, installed the debug APK on device 859013c6, launched com.ledgr.timetable, and manually navigated through every screen and back-home exit with ADB/UIAutomator title checks.
T0.4 - Built the Material 3 Expressive theme from docs/mockups.html tokens: lavender tonal palette, 20dp/28dp rounded shapes, pill buttons, compact 19/14/12sp type scale, and a themed scaffold sample with cards, chips, progress, and action buttons. Verified :timetable-app:assembleDebug, :timetable-app:connectedDebugAndroidTest, installed/launched on device 859013c6, captured the app screenshot, rendered the mockup Home panel from docs/mockups.html, and performed the side-by-side visual check for matching palette, radius, and type rhythm.
T1.1 - Built the Room-backed Home screen with institute creation and institute rows that can open the build placeholder or roster settings. Wrote the roster DAO test first and observed the expected compile failure on missing DAO methods, then verified :timetable-app:assembleDebug, :timetable-app:connectedDebugAndroidTest, installed on device 859013c6, created Bucket1Academy, force-stopped and relaunched com.ledgr.timetable, and confirmed the institute still appeared from persisted Room data.
T1.2 - Built teacher roster add/edit/remove for the selected institute, backed by Flow queries scoped to instituteId through TimetableRepository and TimetableViewModel. Verified the DAO query/update/delete path with RosterDaoTest, then manually added MsRaoB1, renamed it to MsRaoB1Updated, added and removed TempTeacherB1, and confirmed the kept teacher still appeared on device 859013c6.
T1.3 - Built section roster add/edit/remove for the selected institute using the same scoped Room flow path as teachers. Verified section query/update/delete by instituteId with RosterDaoTest, then manually added Grade6AB1, renamed it to Grade6AB1Updated, added and removed TempSectionB1, and confirmed the kept section still appeared on device 859013c6.
Bucket 1 isolation - Added a connected instrumentation check that creates Bucket1Academy and Bucket1Second through TimetableRepository, adds one teacher and one section to each, and verifies each institute's queried roster contains only its own rows and excludes the other institute's rows. Ran :timetable-app:connectedDebugAndroidTest on device 859013c6 and confirmed 3 tests passed.
